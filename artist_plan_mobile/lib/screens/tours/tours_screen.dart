
import 'package:flutter/material.dart';

class ToursScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tours'),
      ),
      body: Center(
        child: Text('Tours Screen'),
      ),
    );
  }
}
